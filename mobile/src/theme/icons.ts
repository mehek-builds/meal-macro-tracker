// ============================================================
// Centralized Lucide line icons (PRD Section 3.3 / 21.6).
// Re-export the icons used so swaps stay in one place.
// Line icons only - never emoji as UI.
// ============================================================

export {
  // Tab bar
  Camera, House, ClipboardList, LineChart, User,
  // Component glyphs
  Plus, ChevronUp, ChevronDown, Droplet, Activity, Pill,
  // Chrome
  X, Moon,
} from 'lucide-react-native';
